// import saveToLocalStorage from "./saveToDb.js";

// const removeData = (item, dataArray) => {
//   const filterdData = dataArray.filter((todo, index) => item !== index);
//   saveToLocalStorage(filterdData);
// };

// export default removeData;